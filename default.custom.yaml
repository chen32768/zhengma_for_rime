patch:
  schema_list:
    # 郑码
    - {schema: zhengma}
    # 郑码打简出繁
    - {schema: zhengma_trad}
    # 郑码拼音
    - {schema: zhengma_pinyin}

  # 配置caps_lock键为中/英切换键
  ascii_composer/good_old_caps_lock: false
  ascii_composer/switch_key:
    Caps_Lock: commit_code
    Shift_L: noop
    Shift_R: noop
    Control_L: noop
    Control_R: noop
